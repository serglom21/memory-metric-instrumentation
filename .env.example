# Sentry project DSN (Settings → Client Keys). Required to emit traces/metrics.
VITE_SENTRY_DSN=

# Optional. Defaults shown in src/sentry.ts if unset.
VITE_SENTRY_RELEASE=memory-metric-instrumentation@0.1.0
VITE_SENTRY_ENVIRONMENT=development
